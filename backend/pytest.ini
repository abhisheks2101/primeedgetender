[pytest]
testpaths = tests ../tests
pythonpath = . ..
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: cross-service integration tests requiring PostgreSQL
