-- PostgreSQL initialization for Tender Intelligence Platform (Module 1)
-- Extensions required by future modules are enabled here at no cost.

CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- pgvector will be enabled in a later module when semantic search is implemented.
-- CREATE EXTENSION IF NOT EXISTS vector;
